export enum EdgeAnimated {
    Yes = 1,
    No = 0,
}

export enum Relationship {
    Spouse = "spouse",
    Child = "child",
    Sibling = "sibling",
    Parents = "parents",
}

export enum AppNodeName {
    memberNode = "member",
    connectorNode = "connector"
}
